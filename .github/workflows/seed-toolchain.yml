name: Seed Toolchain Cache

on:
  workflow_dispatch:

jobs:
  seed:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq build-essential gcc-multilib g++-multilib \
            libncurses-dev libssl-dev libelf-dev zlib1g-dev bison flex git unzip wget \
            python3 python3-pip python3-pyelftools rsync file curl gettext \
            libgmp3-dev libmpc-dev libmpfr-dev patch bc lz4 zstd xz-utils jq clang llvm lld

      - name: Clone source & feeds
        run: |
          git clone --depth 1 --branch v25.12.4 https://github.com/openwrt/openwrt.git openwrt
          cd openwrt && ./scripts/feeds update -a && ./scripts/feeds install -a

      - name: Configure target
        run: |
          cd openwrt
          echo "CONFIG_TARGET_x86=y" > .config
          echo "CONFIG_TARGET_x86_64=y" >> .config
          make defconfig

      - name: Compile tools & toolchain
        run: |
          cd openwrt
          make tools/compile -j$(nproc)
          make toolchain/compile -j$(nproc)

      - name: Cache (toolchain + ccache)
        uses: stupidloud/cachewrtbuild@main
        with:
          ccache: true
          toolchain: true
          skip: true
          prefix: openwrt
          mixkey: x86-64
